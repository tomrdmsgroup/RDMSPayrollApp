# -----------------------------------------------------------------------------
# RDMS Payroll App (Server) - Example Environment File
#
# Copy this file to: server/.env
# Then fill in the values you need.
#
# IMPORTANT:
# - The server loads env vars ONLY from server/.env (see server/src/config.js)
# - Providers in this repo read the variables as named below.
# -----------------------------------------------------------------------------

# Server
PORT=3000

# Database connection string (PostgreSQL)
# Note: not all code paths use this yet, but Prisma and future persistence will.
DATABASE_URL=postgres://user:password@localhost:5432/rdms_payroll

# -----------------------------------------------------------------------------
# Airtable (global + per-base overrides)
#
# Some providers will use the per-base API key if set; otherwise they fall back
# to AIRTABLE_API_KEY.
# -----------------------------------------------------------------------------
AIRTABLE_API_KEY=

# Vitals base/table
AIRTABLE_VITALS_BASE=
AIRTABLE_VITALS_API_KEY=
AIRTABLE_VITALS_TABLE=Vitals

# Payroll Calendar base/table
AIRTABLE_PAYROLL_CALENDAR_BASE=
AIRTABLE_PAYROLL_CALENDAR_API_KEY=
AIRTABLE_PAYROLL_CALENDAR_TABLE=Payroll Calendar

# -----------------------------------------------------------------------------
# Toast
#
# Current code uses a single API key (TOAST_API_KEY), not OAuth client id/secret.
# -----------------------------------------------------------------------------
TOAST_API_KEY=
# Optional overrides (safe to leave blank)
TOAST_BASE_URL=
TOAST_LOCATION_ID=

# -----------------------------------------------------------------------------
# Email (SMTP)
#
# Current code uses SMTP_* variables (server/src/providers/emailProvider.js).
# -----------------------------------------------------------------------------
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
SMTP_FROM=ops@example.com

# -----------------------------------------------------------------------------
# Asana
# -----------------------------------------------------------------------------
ASANA_ACCESS_TOKEN=
ASANA_PROJECT_ID=
# Optional override
ASANA_BASE_URL=

# -----------------------------------------------------------------------------
# 911 notification
# -----------------------------------------------------------------------------
FAILURE_EMAIL=911@rdmsgroup.com

# -----------------------------------------------------------------------------
# App auth / bootstrap admin
#
# APP_JWT_SECRET is reserved for future auth hardening; current code primarily
# relies on the default admin bootstrap values below.
# -----------------------------------------------------------------------------
APP_JWT_SECRET=change-me
APP_DEFAULT_ADMIN_EMAIL=admin@example.com
APP_DEFAULT_ADMIN_PASSWORD=changeme
